const PREFIX = '#';
const STATUS = ['in-progress', 'done', 'abandoned'];
const PROJECT_PROPERTIES = ['deadline', 'name', 'status'];
const DEFAULT_DATE = '1999-01-01';

const DEFAULT_HOST = 'http://localhost:5000';

module.exports = { PREFIX, STATUS, DEFAULT_DATE, DEFAULT_HOST, PROJECT_PROPERTIES };